server=http://localhost:4111
server_api_version=/api/v1

UI_BASE_URL=http://localhost:3000
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
ADMIN_ROLE=ADMIN
PYTEST_SEED=123

# Database (for with_database backend)
# You can override any of these via environment variables by editing Config.get(...) defaults in code if needed.
DB_HOST=localhost
DB_PORT=5433
DB_NAME=nbank
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Pre-created test user in with_database backend (used to avoid admin-create-user dependency)
TEST_USER_USERNAME=user
TEST_USER_PASSWORD=user